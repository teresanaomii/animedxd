<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- Mask ripple, tetap rounded -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="15dp" />
        </shape>
    </item>

    <!-- Isi tombol dengan selector untuk semua state -->
    <item>
        <selector>
            <!-- State ketika tombol ditekan -->
            <item android:state_pressed="true">
                <shape android:shape="rectangle">
                    <solid android:color="#163A9B" /> <!-- Biru gelap -->
                    <corners android:radius="15dp" />
                </shape>
            </item>

            <!-- State ketika tombol fokus -->
            <item android:state_focused="true">
                <shape android:shape="rectangle">
                    <solid android:color="#1E4BBE" /> <!-- Biru normal -->
                    <corners android:radius="15dp" />
                </shape>
            </item>

            <!-- State ketika tombol disabled -->
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <solid android:color="#9CB3E9" /> <!-- Biru pucat -->
                    <corners android:radius="15dp" />
                </shape>
            </item>

            <!-- State default -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#1E4BBE" /> <!-- Biru normal -->
                    <corners android:radius="15dp" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
